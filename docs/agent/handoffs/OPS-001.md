# OPS-001 handoff

TASK: Structured logging and correlation IDs; trace requests through existing calls.
STATUS: Ready for review; focused/full tests and independent review passed.
WHAT WAS DONE: Read Jira and 17 section 1, existing infrastructure, security filters,
logging and transaction boundary. Added before-auth servlet correlation filter,
safe incoming ID validation/generation/echo, finally-scoped MDC, template-only
request metadata, built-in JSON console/file logging, event append attempts and
actual transaction completion outcomes. No new logging dependency or domain writes.

FILES CHANGED:
- backend/src/main/java/com/atlas/backend/logging/CorrelationIdFilter.java
- backend/src/main/java/com/atlas/backend/event/JpaEventRepository.java
- backend/src/main/resources/application.yml
- backend/src/test/java/com/atlas/backend/logging/CorrelationIdFilterTest.java
- backend/src/test/java/com/atlas/backend/logging/RequestLoggingIntegrationTest.java
- docs/engineering/17_OPERATIONS_AND_DEPLOYMENT.md: concrete logging mechanics.
- docs/agent/DECISION_LOG.md: DEC-0015.
- docs/agent/handoffs/OPS-001.md: this record.
DATABASE CHANGES: None; event payloads/schema unchanged.
API CHANGES: X-Correlation-ID response header, including auth failures. Existing
response bodies/statuses unchanged. Missing/unsafe/multiple IDs generate a UUID.
TESTS RUN: From backend: mvn test '-Dtest=CorrelationIdFilterTest,RequestLoggingIntegrationTest,EventTransactionIntegrationTest,EventRepositoryAppendOnlyTest'.
TEST RESULTS: Initial run 43 tests, 0 failures, 2 errors. Actual JSON encoder rejected
duplicate correlationId (both MDC and key/value). Removed redundant key/value;
rerun: 43 tests, 0 failures, 0 errors, 0 skipped; BUILD SUCCESS. Tests include real HTTP/production encoder, rollback, auth rejection,
MDC cleanup/restoration, invalid IDs, no body/token/query disclosure, and EVT-002.
KNOWN FAILURES: None after fixing the duplicate-field defect and rerunning.
KNOWN RISKS: Current calls are synchronous. JpaTransactionManager completion callbacks
execute on the same thread before request return, retaining MDC. Future async work
or outbound HTTP/AI clients must explicitly propagate context; none currently exist.
Existing third-party/application exception logging is preserved; no new request-body,
token, query-string, event reason/payload logging is added.
FULL BATCH VERIFICATION: mvn test from backend — 188 tests, 0 failures/errors/skips,
BUILD SUCCESS. Independent final integration review PASS; human verification pending.
OPEN QUESTIONS: None blocking OPS-001.
ARCHITECTURAL CONCERNS: No global observability/auth-audit redesign. No Stage 7 ranking,
event history mutation, or scheduling behavior changes.
REVIEW: Independent review found duplicate-field defect, agreed current synchronous
MDC propagation is sufficient. Independent re-review PASS after 43 passing tests.
NEXT STEP: Batch PR review and human verification; do not merge.
DO NOT REPEAT: Do not put a field in both MDC and SLF4J key/values; Boot's encoder
rejects duplicate JSON names. Do not log raw URI/query/body/header contents.
