TASK
Jira key: DOM-006
Title: Fixed Commitment / Calendar Event entity
Objective: Deliver user-owned Fixed Commitment persistence and authenticated manual CRUD with Fixed/source invariants, null-only recurrence input, physical deletion, and atomic immutable Event Log entries.

STATUS
[ ] Not started
[ ] In progress
[ ] Blocked
[x] Ready for review
[ ] Complete

WHAT WAS DONE
- Created branch `feature/DOM-006-fixed-commitment` from develop at `1e7f832` (develop matched origin/develop before branching).
- Added Fixed Commitment entity, owner-scoped repository, service, controller, DTOs, validation, and exception handling.
- Enforced server-controlled `source=manual` on manual creation; persistence supports `manual` and `screenshot_import`.
- Implemented null-only public recurrence behavior; non-null recurrence input is rejected.
- Allowed overlapping reservations and repeated POST creates distinct entities.
- Implemented physical DELETE with `204 No Content` and immutable deletion snapshot event.
- Added payload support to the existing Event entity/factory.
- Added V8 migration and pinned historical Category migration test to V7.
- Updated API, domain, database, event, traceability and decision documentation.

FILES CHANGED
- `backend/src/main/java/com/atlas/backend/fixedcommitment/*` — DOM-006 domain, persistence, API, validation and errors.
- `backend/src/main/java/com/atlas/backend/event/Event.java` — event payload accessor/factory overload.
- `backend/src/main/resources/db/migration/V8__create_fixed_commitments.sql` — Fixed Commitment schema and constraints.
- `backend/src/test/java/com/atlas/backend/fixedcommitment/*` — unit, API/authorization, transaction/concurrency and migration tests.
- `backend/src/test/java/com/atlas/backend/category/CategoryMigrationIntegrationTest.java` — historical V7 pinning.
- `docs/engineering/02_DOMAIN_MODEL_AND_STATE_MACHINES.md` — approved DOM-006 boundary.
- `docs/engineering/03_REQUIREMENTS_TRACEABILITY.md` — DOM-006 implementation status and boundaries.
- `docs/engineering/10_EVENT_LOG.md` — DOM-006 event contract.
- `docs/engineering/12_API_SPECIFICATION.md` — implemented §5.1 CRUD contract.
- `docs/engineering/13_DATABASE_SPECIFICATION.md` — V8 physical mapping and deletion/transaction rules.
- `docs/agent/DECISION_LOG.md` — DEC-0008 approved DOM-006 scope.
- `docs/agent/handoffs/DOM-006.md` — this handoff.
- `docs/agent/ATLAS_CURRENT_STATE.md` — pre-existing user modification was preserved and is not part of this implementation commit.

DATABASE CHANGES
- Added V8 `fixed_commitments` table with owner FK, source allow-list, positive interval check, nullable recurrence storage, and `(user_id, start_time, end_time)` index.
- V1–V7 were not modified.
- Fresh and populated V7→V8 migration tests pass on H2 and isolated MySQL 8.0.46.

API CHANGES
- Added authenticated `POST /fixed-commitments`.
- Added authenticated `GET /fixed-commitments/{id}`.
- Added authenticated `PATCH /fixed-commitments/{id}`.
- Added authenticated `DELETE /fixed-commitments/{id}` returning 204.
- No collection/range, import, scheduling, recovery, recurrence or idempotency endpoint was added.

TESTS RUN
- `mvn '-DargLine=-Djdk.net.URLClassPath.disableClassPathURLCheck=true' '-Dtest=FixedCommitment*Test,CategoryMigrationIntegrationTest' test` from `backend`.
- `mvn '-DargLine=-Djdk.net.URLClassPath.disableClassPathURLCheck=true' verify` from `backend`.
- Focused MySQL API: `mvn ... '-Dtest=FixedCommitmentIntegrationTest' ... test` against isolated MySQL 8.0.46 on port 13316.
- Focused MySQL migration: `mvn ... '-Dtest=FixedCommitmentMigrationIntegrationTest' ... test` against isolated MySQL 8.0.46.
- `npm run build` from `frontend`.
- `git diff --check`.

TEST RESULTS
- Focused H2 DOM-006/category run: 18 tests, 0 failures, 0 errors, 0 skipped.
- Full backend Maven verify: 68 tests, 0 failures, 0 errors, 0 skipped; BUILD SUCCESS.
- Isolated MySQL API run: 7 tests, 0 failures, 0 errors, 0 skipped; BUILD SUCCESS.
- Isolated MySQL migration run: 2 tests, 0 failures, 0 errors, 0 skipped; BUILD SUCCESS.
- Frontend build: Vite production build succeeded (32 modules transformed).
- `git diff --check`: passed.

KNOWN FAILURES
- None in the recorded test runs.
- A sandboxed initial frontend build hit `spawn EPERM`; rerunning the same build with the required execution allowance passed. This is an environment restriction, not a code failure.

KNOWN RISKS
- Aiven verification was not run because `AIVEN_DB_PASSWORD` is unavailable in the environment. Isolated MySQL 8.0.46 verification passed instead.
- Recurrence is storage-only and public non-null values are rejected by design; a future story must define recurrence semantics before enabling them.
- Scheduling/recovery/import integration is deliberately absent.
- The local MySQL verification server was started in `backend/target/dom006-mysql`; it is not a repository file.

OPEN QUESTIONS
- No product decision remains for DOM-006. Future recurrence, scheduling/recovery and screenshot import contracts belong to later stories.

ARCHITECTURAL CONCERNS
- `05_RESCHEDULING_AND_RECOVERY.md` still has historical documentation around Fixed collision behavior; DOM-006 intentionally does not implement that layer.
- The pre-existing `docs/agent/ATLAS_CURRENT_STATE.md` working-tree change was not modified or staged.

NEXT STEP
- Independent Review Agent should verify the DOM-006 diff, API contract, ownership behavior, migration safety, transaction tests, documentation scope, and the Aiven verification limitation.

DO NOT REPEAT
- Do not add recurrence syntax/expansion, overlap detection, screenshot/OCR import, scheduler/recovery calls, collection endpoints, or persisted idempotency infrastructure in DOM-006.
