# SCH-005 Handoff — Stage 3 User Importance Tier

TASK
Jira key: SCRUM-38 (SCH-005)
Title: Stage 3 user importance tier
Objective: Implement Stage 3 user-defined importance tier comparison logic for candidate items (CRITICAL > HIGH > MEDIUM > LOW) consuming stored Commitment importance.

STATUS
[X] Ready for review

WHAT WAS DONE
- Implemented `ImportanceTier` enum (`CRITICAL` [4], `HIGH` [3], `MEDIUM` [2], `LOW` [1]) with string parsing helper `from(String)`.
- Implemented `Stage3UserImportanceTier` comparator / evaluator handling pure Stage 3 tier comparisons:
  - Higher rank outranks lower rank (`CRITICAL` > `HIGH` > `MEDIUM` > `LOW`).
  - Equal tiers evaluate to `0` (tied at Stage 3).
  - Consumes stored `Commitment.getImportance()` directly without querying or re-evaluating `Category.default_importance`.
  - Implements `Comparator<Commitment>` sorting in descending order of importance tier.
- Created `Stage3UserImportanceTierTest` verifying enum parsing/ranking, tier ordering, tie-breaking equality, determinism, stored Commitment importance invariance to Category mutations, and comparator collection sorting.

FILES CHANGED
- `backend/src/main/java/com/atlas/backend/scheduling/ImportanceTier.java` (Enum defining importance tiers and rank values)
- `backend/src/main/java/com/atlas/backend/scheduling/Stage3UserImportanceTier.java` (Pure Stage 3 user importance tier comparison logic and comparator)
- `backend/src/test/java/com/atlas/backend/scheduling/Stage3UserImportanceTierTest.java` (Focused unit test suite for SCH-005)
- `docs/agent/handoffs/SCH-005.md` (Handoff document)

DATABASE CHANGES
- None

API CHANGES
- None

TESTS RUN
- `mvn test -Dtest=Stage3UserImportanceTierTest` in `backend/`
- `mvn test` in `backend/`

TEST RESULTS
- Focused SCH-005 tests: 6 tests run, 0 failures, 0 errors, 0 skipped (PASS)
- Full backend regression suite: 95 tests run, 0 failures, 0 errors, 0 skipped (BUILD SUCCESS)

KNOWN FAILURES
- None

KNOWN RISKS
- None. Stage 3 logic is completely pure and isolated.

OPEN QUESTIONS
- None

ARCHITECTURAL CONCERNS
- None. Responsibility boundary strictly respects DOM-003 stored commitment importance and 04 §2.

NEXT STEP
- Review `feature/SCH-005-importance-tier` branch for merge into `develop`.

DO NOT REPEAT
- Do not attempt to re-read `Category.default_importance` dynamically inside Stage 3 comparison logic — DOM-003 handles category defaults at Commitment create/update time.
