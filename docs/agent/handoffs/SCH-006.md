# SCH-006 Handoff — Stage 4 Goal Importance & At-Risk Status

TASK
Jira key: SCRUM-39 (SCH-006)
Title: Stage 4 goal/at-risk boost
Objective: Implement Stage 4 (Goal Importance & At-Risk Status) protective boost tier evaluation and comparison logic for tasks linked to at-risk Goals.

STATUS
[X] Ready for review

WHAT WAS DONE
- Implemented `Stage4GoalAtRiskTier` in `com.atlas.backend.scheduling`:
  - Defined `Status` enum (`NORMAL`, `BOOSTED`) without arbitrary numeric constructor weights.
  - Implemented `evaluateStatus(Goal)` evaluating `Goal.AT_RISK` planning state (`"at_risk"`).
  - Implemented `evaluateStatus(Long goalId, Predicate<Long> isGoalAtRiskPredicate)` and `evaluateStatus(Commitment, Predicate<Long>)` with strict null checks (`Objects.requireNonNull` for predicate and commitment).
  - Implemented `compareStatuses(Status, Status)` and `compareCommitments(Commitment, Commitment, Predicate<Long>)` returning standard comparator ordering (`BOOSTED` > `NORMAL`).
  - Implemented `Comparator<Commitment>` instance for collection sorting (placing `BOOSTED` tasks first).
- Created `Stage4GoalAtRiskTierTest` testing Goal state evaluation, Predicate lookup, null guards, status ordering, commitment comparison, determinism, and collection sorting.

FILES CHANGED
- `backend/src/main/java/com/atlas/backend/scheduling/Stage4GoalAtRiskTier.java` (Pure Stage 4 evaluation and comparison logic)
- `backend/src/test/java/com/atlas/backend/scheduling/Stage4GoalAtRiskTierTest.java` (Focused unit test suite for SCH-006)
- `docs/agent/handoffs/SCH-006.md` (Handoff document)

DATABASE CHANGES
- None

API CHANGES
- None

TESTS RUN
- `mvn test -Dtest=Stage4GoalAtRiskTierTest` in `backend/`
- `mvn test` in `backend/`

TEST RESULTS
- Focused SCH-006 tests: 7 tests run, 0 failures, 0 errors, 0 skipped (PASS)
- Full backend regression suite: 102 tests run, 0 failures, 0 errors, 0 skipped (BUILD SUCCESS)

KNOWN FAILURES
- None

KNOWN RISKS
- None. Stage 4 logic is completely pure, side-effect-free, and isolated.

OPEN QUESTIONS
- None

ARCHITECTURAL CONCERNS
- None. RESC-007 risk calculation is stubbed per specification; Stage 4 consumes Goal planning state/predicate as frozen input.

NEXT STEP
- Review `feature/SCH-006-at-risk-boost` branch for merge into `develop`.

DO NOT REPEAT
- Do not add numeric weight fields (e.g. `NORMAL(1)`, `BOOSTED(2)`) to `Status` enum.
- Do not silently default a null `Predicate` parameter to `NORMAL` — require a non-null predicate.
