# DOM-007 Handoff — Dependency modeling

TASK
Jira key: SCRUM-29 (DOM-007)
Title: Dependency modeling
Objective: Persist directed Commitment dependencies with cycle detection on create and the C1 HTTP surface.

STATUS
[X] Ready for review

WHAT WAS DONE
- Added Flyway V10 `commitment_dependency` (two FKs, unique edge, self-edge CHECK, reverse index, no cascade).
- Implemented `com.atlas.backend.dependency` create/list/delete, unbounded cycle detection on write, owner-row serialization, atomic `task.dependency_added` / `task.dependency_removed`.
- Duplicate POST returns the existing edge with HTTP 200 and no second event.
- Added internal bounded `DependencyLookahead` for SCH-007 (truncation and cycle termination reported; truncated results are not treated as “no downstream”).
- Recorded DEC-0010 and documented the API/event/schema contract.

FILES CHANGED
- `backend/src/main/resources/db/migration/V10__create_commitment_dependency.sql`
- `backend/src/main/java/com/atlas/backend/dependency/*`
- `backend/src/test/java/com/atlas/backend/dependency/*`
- `docs/engineering/12_API_SPECIFICATION.md`
- `docs/engineering/10_EVENT_LOG.md`
- `docs/engineering/13_DATABASE_SPECIFICATION.md`
- `docs/engineering/03_REQUIREMENTS_TRACEABILITY.md`
- `docs/agent/DECISION_LOG.md`
- `docs/agent/handoffs/DOM-007.md`

DATABASE CHANGES
- V10 additive table only. V1–V9 unchanged. No automatic destructive rollback; drop the unused table only if deploying a rollback of this unused feature.

API CHANGES
- POST `/commitments/{id}/dependencies` 201/200
- GET `/commitments/{id}/dependencies` 200 array
- DELETE `/commitments/{id}/dependencies/{blockingCommitmentId}` 204
- Documented in `12` under Approved DOM-007 (DEC-0010)

TESTS RUN
- `mvn test -Dtest=DependencyLookaheadTest,DependencyIntegrationTest,DependencyTransactionIntegrationTest,DependencyMigrationIntegrationTest` in `backend/`
- `mvn test` in `backend/`

TEST RESULTS
- Focused DOM-007 tests: 17 tests run, 0 failures, 0 errors, 0 skipped (PASS)
- Full backend regression suite: 119 tests run, 0 failures, 0 errors, 0 skipped (BUILD SUCCESS)

KNOWN RISKS
- Real Aiven/MySQL concurrent cycle test is not run in this agent session unless isolated MySQL is available.

OPEN QUESTIONS
- None for DOM-007 AC. Remaining-effort units remain unspecified for SCH-007.

ARCHITECTURAL CONCERNS
- None. Cancellation impact and Stage 5 ranking are later stories.

NEXT STEP
- Independent review of `feature/DOM-007-dependency-modeling`; then SCH-007 on a follow-up branch.

DO NOT REPEAT
- Do not accept a cyclic write because a depth bound was reached.
- Do not add transitive graphs to CommitmentResponse.
- Do not invent cancellation cascades or work-state changes from dependency edges.
